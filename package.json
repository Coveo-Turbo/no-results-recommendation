{
    "name": "@coveops/no-results-recommendations",
    "version": "1.0.1",
    "description": "Adding recommendations on no results page",
    "main": "dist/index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "coveops build NoResultsRecommendation --template typescript",
        "watch": "coveops build NoResultsRecommendation --template typescript --watch",
        "serve": "coveops serve",
        "precompile": "npm run build"
    },
    "keywords": [],
    "author": "",
    "license": "Apache-2.0",
    "types": "dist/index.d.ts",
    "files": [
        "dist"
    ],
    "dependencies": {
        "@coveops/turbo-core": "^1.1.1",
        "coveo-search-ui": "^2.10082.5"
    },
    "devDependencies": {
        "@coveops/cli": "^0.14.0"
    }
}
